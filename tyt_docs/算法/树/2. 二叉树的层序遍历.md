## 层序遍历

### 思路：
层序遍历是bfs的思想，也可以用队列来进行实现。
[层序遍历过程图](https://file1.kamacoder.com/i/algo/102%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E5%B1%82%E5%BA%8F%E9%81%8D%E5%8E%86.gif)


### 层序遍历：
#### 队列法
```python
  class Solution:
    def layerTraverse(root):
      if root: queue = collections.deque([root])
      result = []
      while queue:
        level = []
        for qu in range(len(queue)):
          cur = queue.popleft()
          level.append(cur.val)
          if cur.left:
            queue.append(cur.left)
          if cur.right:
            queue.append(cur.right)
        result.append(level)
      return result
```
#### 递归法
```python
  class Solution:
    def layerTraverse(root):

      if not root: return []

      levels = []
      def bfs(node, level):
        if not node: return 
        if len(levels) == level:
          levels.append([])
        levels[level].append(node.val)
        if node.left:
          bfs(node.left, level + 1)
        if node.right:
          bfs(node.right, level + 1)
      
      bfs(root, 0)
      return levels
```

### 题目练习：

**107.二叉树的层次遍历 II**
```python
class Solution(object):
    def levelOrderBottom(self, root):
        if not root: return []
        levels = []
        def bfs(node, level):
            if not node: return
            if len(levels) == level: levels.append([])

            levels[level].append(node.val)

            bfs(node.left, level + 1)
            bfs(node.right, level + 1)

        bfs(root, 0)
        return levels[::-1]
```

**199.二叉树的右视图**
```python
class Solution(object):
    def rightSideView(self, root):
        """
        :type root: Optional[TreeNode]
        :rtype: List[int]
        """

        # 这里deque的时候需要判断root
        # if not root :return []
        # queue = deque([root])
        # 如上述来进行书写也可以哦！
        queue = deque([root]) if root else deque()
        result = []
        while queue:
            level = []

            for _ in range(len(queue)):
                node = queue.popleft()
                level.append(node.val)
                if node.left: queue.append(node.left)
                if node.right: queue.append(node.right)
            result.append(level[len(level)-1])
        return result
```